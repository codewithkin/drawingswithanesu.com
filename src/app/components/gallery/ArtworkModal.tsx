"use client";

import React from "react";

export default function ArtworkModal() {
    return (
        <div id="artwork-modal" role="dialog" aria-modal="true">
            {/* Artwork modal placeholder — enlarged image, details, navigation */}
        </div>
    );
}
